@{
    // Set the title of the page as "Main Form".
    ViewData["Title"] = "UploadFile Form";

    // Specify the layout to be used for this view.
    Layout = "_Layout";
}

@await Html.PartialAsync("~/Views/Shared/Forms/UploadFileForm.cshtml")